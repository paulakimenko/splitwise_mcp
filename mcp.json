{
  "mcpServers": {
    "splitwise": {
      "command": "python",
      "args": [
        "-m",
        "app.main"
      ],
      "env": {
        "SPLITWISE_API_KEY": "${SPLITWISE_API_KEY}"
      }
    }
  }
}